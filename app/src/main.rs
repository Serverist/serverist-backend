mod serverist;
pub mod schema;
pub mod models;

use diesel::prelude::*;
use std::future::Future;

use actix_web::{HttpRequest, HttpResponse, Error, get, Responder};

fn main() {
    diesel::update(users.)
    println!("Hello, world!");
}
